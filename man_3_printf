.\" Manpage for Holberton Students.

.TH man 3 "19 March 2019" "1.0" "_printf man page"

.SH NAME
_printf \- A function that produces output according to a format.

.SH SYNOPSIS
       #include "holberton.h""

       int _printf(const char *format, ...);

.SH DESCRIPTION
	The function allows the user to print a string of characters. Additionally the function allows to print the value of the variables of the program through the use of the character%. That makes a call to the variable type and prints the value that the variable has.
	For each type of variable it is necessary to clarify the type of variable that is being called.
	% c for a character.
	% s for a string
	% d for decimal.

.SH RETURN VALUE
         Return the number ot characters printed excluding the nukk byte.

.SH AUTHOR
         Giovani Rubio, Camilo Maldonado.
