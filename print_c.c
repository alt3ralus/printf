#include "holberton.h"
#include <stdlib.h>

/**
 * print_char - print char
 * @c
 *
 * Return:
 */
void print_char(va_list arg)
{
	char pc = (char, va_arg(arg, int));
	_putchar(pc);
}


/**
 * print_char - print char
 * @c
 *
 * Return:
 */
void print_string(va_list arg)
{
	int i;
	char *str = va_arg(s, char *);

	switch (!p + 0)
	{
		case 1:
			printf("%p", p);
			break;
		default:
			printf("%s", p);
	}
										
}////
